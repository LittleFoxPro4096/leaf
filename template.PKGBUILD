pkgname=""
pkgver=""
pkgdesc=""
homepage=""
license=("")
sources=("${pkgname}-${pkgver}.tar.xz")

src_prepare() {
  tar -xf ${DIST_DIR}/${pkgname}-${pkgver}/${sources[0]} --strip-components=1
}

src_build() {
  :
}
